# Спецификация к Заданию 1

## 1. Цель модуля
Автоматизация учёта заявок на ремонт бытовой техники в сервисном центре с возможностью сбора и отображения аналитической статистики по выполненным работам.

## 2. Входные данные
Входные данные поступают из реляционной базы данных SQLite, первоначально загруженные из файлов заказчика:

- `inputDataRequests.xlsx` → таблица `requests`
- `inputDataUsers.xlsx` → таблица `users`

### Структура таблицы `requests` (актуальные поля):

| Поле | Тип | Описание |
| :--- | :--- | :--- |
| `requestID` | INTEGER (PK) | Уникальный идентификатор заявки |
| `startDate` | VARCHAR | Дата регистрации заявки (ГГГГ-ММ-ДД) |
| `applianceType` | VARCHAR | Тип бытовой техники (например, «Холодильник», «Фен») |
| `applianceModel` | VARCHAR | Модель устройства |
| `problemDescryption` | TEXT | Описание неисправности |
| `requestStatus` | VARCHAR | Статус заявки: «Новая заявка», «В процессе ремонта», «Готова к выдаче» |
| `completionDate` | VARCHAR | Дата завершения ремонта (ГГГГ-ММ-ДД или NULL) |
| `clientID` | INTEGER (FK → users.userID) | Идентификатор заказчика |

> ⚠️ **Примечание:** поле `homeTechType` из исходного Excel было переименовано в `applianceType` в соответствии с соглашением об именовании (snake_case → camelCase не требуется, но используется осмысленное наименование на английском языке).

## 3. Выходные данные
Программный модуль вычисляет следующую статистику на основе записей со статусом **"Готова к выдаче"** и отображает её на странице `/statistics` (доступно только пользователям с ролями «Администратор» и «Менеджер»):

| Показатель | Описание | Формат отображения |
| :--- | :--- | :--- |
| **Количество выполненных заявок** | Число записей, где `requestStatus = "Готова к выдаче"` | Целое число |
| **Среднее время выполнения заявки** | Среднее значение в днях между `startDate` и `completionDate` по всем завершённым заявкам | Число с округлением до 2 знаков после запятой (например, 4.67 дней) |
| **Топ-3 типа бытовой техники** | Наиболее частые значения поля `applianceType` среди завершённых заявок | Список: 1. Холодильник — 2 заявки, 2. Фен — 2 заявки, 3. ... |

📌 Все расчёты выполняются динамически при каждом запросе к `/statistics`, на основе актуальных данных из БД.

## 4. Ограничения и условия
- Заявки со статусом, отличным от **"Готова к выдаче"**, не учитываются в расчётах.
- Если `completionDate` отсутствует (равен NULL), даже при статусе "Готова к выдаче", запись игнорируется (защита от некорректных данных).
- **При отсутствии завершённых заявок:**
    - Количество = 0
    - Среднее время = —
    - Топ типов = Нет данных

## 5. Источник данных в коде
- **Модель:** `app.models.RepairRequest`
- **CRUD-функция:** `app.crud.requests.get_requests(db)`
- **Сервис расчёта:** `app.services.statistics`
- **Шаблон вывода:** `app/frontend/templates/statistics.html`
